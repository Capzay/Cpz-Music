# Optional release signing. Copy to keystore.properties and fill in.
# Never commit keystore.properties or the .jks / .keystore file.
storeFile=cpz-release.keystore
storePassword=
keyAlias=cpz
keyPassword=
