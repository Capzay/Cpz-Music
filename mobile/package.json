{
  "name": "cpz-music-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Android wrapper for Cpz Music, with lock screen media controls",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "doctor": "./scripts/apk.sh doctor",
    "apk": "./scripts/apk.sh debug",
    "apk:install": "./scripts/apk.sh install",
    "apk:release": "./scripts/apk.sh release",
    "build:debug": "./scripts/apk.sh debug",
    "build:release": "./scripts/apk.sh release"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5"
  },
  "overrides": {
    "@xmldom/xmldom": "^0.9.12"
  }
}
